@require '~vuepress-plugin-tabs/dist/themes/default.styl'

.photo-inline {
  &__image {
    width: 300px;
    float:left;
  }
  &__text {
    display:inline;
  }
}

.language-.extra-class {
    background-color:#30336b;
}
